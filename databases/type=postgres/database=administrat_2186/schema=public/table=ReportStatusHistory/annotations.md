---
type: manual
comment: These are manual notes you want the agent to keep in mind, safe to edit.
---

Les transitions `DELETED` sont écrites par la purge automatique, et une partie des transitions
`CLOSED` par le cron de clôture automatique (voir `Report/annotations.md`). Aucune des deux ne
reflète une action humaine.
